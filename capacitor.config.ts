import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hybridmusic.client',
  appName: 'hybrid-music-client',
  webDir: 'out'
};

export default config;
